import { environment } from '../../environments/environment';

export const LoginUrls = {
  ingresar: `${environment.apiUrl}/login`,
  registrarCliente: `${environment.apiUrl}/registrar/clientes`,
};
